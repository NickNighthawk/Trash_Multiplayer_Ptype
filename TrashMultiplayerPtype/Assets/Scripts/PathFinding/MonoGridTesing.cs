using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MonoGridTesing : MonoBehaviour
{
    // Start is called before the first frame update

    void Start()
    {
        GridTest grid = new GridTest(2, 2, 2);
    }

}
